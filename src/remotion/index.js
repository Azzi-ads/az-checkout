// Ponto de entrada do Remotion (separado do main.jsx do Vite).
// Rode o estúdio com: npm run video
import { registerRoot } from 'remotion';
import { RemotionRoot } from './Root.jsx';

registerRoot(RemotionRoot);
